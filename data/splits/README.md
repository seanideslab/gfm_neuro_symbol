# Scenario Split Status

The complete scenario split membership is not included in this preparation release. `scenario_ids_TEMPLATE.csv` is a schema placeholder and is not evidence. Before archival, replace it with `scenario_ids.csv` containing every real scenario and trajectory ID, its split, and the metadata needed to reproduce the split.